from os import cpu_count
from lights import *
from tqdm import tqdm
# pro_cam_calibrate(wait_time=0.0)

# calibrate_gamma(wait_time=0.0)
# exposure_times = [-4,-3,-2,-1,0,1]

# HDR_gamma(wait_time=0,exp_val=exposure_times)

# calibrate projector gamma
# Capture(fps=0.4,exp_val=-5).calibrate_gamma(test=True)

# Capture().decode_gamma(test=True)

# Capture().HDR_capture(capture=False)

# post HDR
# HDR_processing()
# Capture().decode_HDR()